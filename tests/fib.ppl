fun fib a:i5, b:i5 => none { # this is a sample comment
    println("$0".fmt(b)) # this too
    if(b >= 1000){
        return(null) # null is a default const
    }
    fib(b, a + b)
}

fun main => none {
    println("1")
    fib(1, 1)
}